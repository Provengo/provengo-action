name: Example Workflow
on: [push]
jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - name: Run your Action
        uses: ./ .github/actions/provengo-action
      - name: Run provengo
        uses: provengo create newProject
